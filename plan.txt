인터페이스랑 Enum은 전부 Value스크립트에
+일단 합 판정(가능한지 등등부터)

이벤트에 InstanceId해서 누가 실행시켰는지 알수있게(실돈 체력회복효과도 이거로 가능할듯)
+ 누가 피해를 입혔는지는 알수있지만 그 캐릭터는 알수없음 피해를 입은정보를 전달할 수단필요
+ 옵저버 패턴???

공격 데미지
{
	데미지 체력감소 
	흐트러짐 게이지 확인
}
에니메이션은 에니메이션에서 직접 스프라이트 변경하지않고(에니메이션에서는 보통 이펙트 파티클 등등)
보통코드함수발동 시키는거로 하거나 에니메이션 대체제를 만들어야할듯(아니면 에니메이션으로하되 빈오브젝트 활성화 비활성화로)
위에서 언급된 발동함수는 state 에 저장할수있게만듬

효과관련 정리
State 패턴 , Observer 패턴 , 오브젝트폴링(state 클래스 리셋코드 작성필요)


효과간섭종류
(
	추가할 함수 받아올곳 : 캐릭터 , 스킬 ,버프디버프	

	코드실행 진행상태에대해 저장하는 Static 변수 생성, 거기에 효과간섭 관련해서 추가
	("[  ]시" 는 Excute 에 "[  ] 전" 같은경우엔 Enter 에 "[  ]후" 같은경우엔 Exit 에)
	리스트나 어디에 모아둘 경우 스스로 빠질수있도록 설정( class로해서 +횟수제한있는효과는 횟수 전부소모시 빠지도록 )
	이전처럼 string Dictionary ? 일단은 string 이 나을듯 성능적으론 enum 일듯 하지만  상황종류가 너무 많은 관계로
	상황예 : 출혈 침잠 파열 등등 디버프 발동시(디버프발동시?(묶어도되나..?) +) , 특정X번째코인 발동시(코인발동시 + if문으로 해결가능) 
	enum으로 시도해보고 가능한 묶어보는걸로
	state 관련 클래스는 평상시 없다가 필요시(이벤트추가시) 에만 생성

	본인스킬(스킬사용시 action 추가)
	{
		기본위력 증가,코인위력증가
	}
	합코드()
	{
		코인갯수 증가, 합간섭(강제 승리패배무승부,최종값 증가가)
	}
	효과간섭(으케하냐)
	{
		화상출혈침잠 등등등 발동시,위력횟수 증가시,일정량 피해를입을시
	}

	상태패턴 써서 현재 합진행 상태판단 및 발동 어떰?
	static 처럼 간섭편하게 혹은 간섭쉽게 interface 로 발동할꺼 추가할수있게
)

옵저버패턴 필요예정 옵저버패턴 등 어떤 방식으로 [  ]시 효과 적용 
[  ] 할경우있을시 발동되는 효과에 if 문 넣기 상태머신처럼 Class혹은 interface 만들어서 발동시키는거 통일 
합 Value_normal :기본값
 {
	합시작전 효과 발동
	+효과 추가할꺼 추가


	공격 기본값 받아오기 : interface int I_Value_NormalCheck()
	코인 갯수 받아오기 : interface int I_Coin_Count()
	공격 기본값 받아오기 : interface int I_Value_NormalCheck()
	코인 갯수 받아오기 : interface int I_Coin_Count()
	반복()
	{
		코인 앞뒤 확인 : interface bool I_Coin_ResultCheck
		코인 값(+확인) 추가 : interface int I_Coin_ValueCheck(bool) 

		코인 앞뒤 확인 : interface bool I_Coin_ResultCheck
		코인 값 추가 : interface int I_Coin_ValueCheck(bool) 
	}
	값 비교 : Value_CompareResult Value_Compare(int,int)  //속도빠른쪽 바탕 승리 패배 출력

	값 비교 승리 패배 무승부 판정
	값 비교 결과에따른 값비교 승리패배무승부 효과 발동

	if(코인남아있을시)
	{
		반복
	}
	else
	{
		합종료
		합종료후 결과에따른 다음 이벤트 연계
	}
 }


합 시스템

공격가능 인터페이스

아래항목 Skill_ 로 묶음

합 가능
일반공격 EGO 등등 공격 , 합가능 반격 방어

합 안함
일반 방어 반격 회피(합 같아 보이지만 아님)
{
	합 가능한지 아닌지 확인(불값 혹은 enum을통해 비교)
	위의 값을 받아가는 인터페이스 필요 
}

방어스킬분류인지 공격스킬분류인지

공격스킬분류 일반공격 공격EGO +(방어스킬EGO 연출만 생각해보면 좋을듯 (라오루 오늘의표정,그.. 반격 징벌새?))
방어스킬분류 일반 방어 반격 회피 , 강화 방어 반격 회피?(강화회피??)

{
	왠만하면 위에처럼 불 값
}

스킬종류 (일반스킬 EGO 방어 등등등)
{
	Enum 값으로 나눠둘 예정
}

합가능인지확인(bool값 확인) -둘다True->  합진행
						-하나False-> 일방공격진행(회피,방어,반격)
				    	-둘다False-> 상쇄

합진행 -> 번갈아가면서 코인계산(속도빠른쪽 우선) -> 



